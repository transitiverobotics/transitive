A simple docker container to simulate a robot that we want to connect to the TR instance running on the dev machine we are running this on.

# Usage

```
./run.sh
```
This builds the image, runs it, and puts you in a shell on it. Proceed by installing the `curl` script to install the transitive agent.
